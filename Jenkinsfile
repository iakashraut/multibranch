pipeline {
    agent {label
    {label'built-in'
    customWorkspace'/mnt/project/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'yum install httpd -y'
            }
        }
    }
}
