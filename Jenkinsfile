pipeline {
    agent {label
    {label'worker'
    customWorkspace'/mnt/project4/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'touch file5'
            }
        }
    }
}
