pipeline {
    agent {label
    {label'built-in'
    customWorkspace'/mnt/project3/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'touch file2'
            }
        }
    }
}
