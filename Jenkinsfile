pipeline {
    agent {label
    {label'built-in'
    customWorkspace'/mnt/project2/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'touch file2'
            }
        }
    }
}
