pipeline {
    agent {label
    {label'slave-1'
    customWorkspace'/mnt/project2/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'touch file2'
            }
        }
    }
}
