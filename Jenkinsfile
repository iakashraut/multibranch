pipeline {
    agent {label
    {label'built-in'
    customWorkspace'/mnt/project4/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'touch file3'
            }
        }
    }
}
