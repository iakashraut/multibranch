pipeline {
    agent {label
    {label'worker'
    customWorkspace'/mnt/project2/'
    }
    }

    stages {
        stage('Hello') {
            steps {
                sh'touch file2'
            }
        }
    }
}
