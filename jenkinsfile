pipeline {
    agent {label
    {label'built-in'
    customWorkspace'/mnt/project'
    }
    }

    stages {
        stage('Hello alex2') {
            steps {
                sh'touch file1'
            }
        }
    }
}
